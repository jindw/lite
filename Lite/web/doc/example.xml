<html xmlns="http://www.w3.org/1999/xhtml"
	xmlns:c="http://www.xidea.org/ns/lite/core">
	<body>
		<!--
			如果您的编辑器没有语法提示，清看设置：
			http://www.javaeye.com/topic/342483
			
			获得语法提示的方法有如下三种：
			1.输入 "<c:",按下 Alt +/ 有可选标记列表
			2.在模版标记内可添加属性的地方，按下Alt + / 有可选属性列表
			3.鼠标放在 模版标记或者标记属性上，有标记描述提示显示
		-->

		<!-- 逻辑控制 -->
		<c:if test="${test}">
			当
			<var>test</var>
			变量为真时，渲染该节点内容
		</c:if>
		<c:else>当前面的if节点不成立时，渲染该节点内容</c:else>

		<c:for var="item" items="${[1,2,3,4]}">
			当前迭代元素为：${item} 、当前循环索引值为：${for.index}
			<c:for var="item2" items="${['a','b','c']}"
				status="status">
				子循环的当前迭代元素为${item2} 、子循环当前索引值为：${status.index}
			</c:for>
			<c:else>子循环没有任何内容（else是可以直接接for的，for没有执行任何循环时，执行else）</c:else>
		</c:for>

		<c:choose>
			<c:when test="${date.getDay()==1}">星期一</c:when>
			<c:when test="${date.getDay()==2}">周二</c:when>
			<c:when test="${date.getDay()==3}">周三</c:when>
			<c:when test="${date.getDay()==4}">周四</c:when>
			<c:when test="${date.getDay()==5}">周五</c:when>
			<c:when test="${date.getDay()==6}">周末</c:when>
			<c:otherwise>周日</c:otherwise>
		</c:choose>


		<!-- 变量输出 和 xml 自动编码 -->
		<div>
			<input value="${unescapedText}" />
			<textarea>${unescapedText}</textarea>
		</div>

		<!-- 表单自动填充(可选功能) -->
		<form>
			<input name="user.name" />
			等价于
			<input name="user.name" value="${user.name}" />
			-----------------
			<select name="user.age">
				<option value="1">一周岁</option>
				<option value="2">两周岁</option>
				....
			</select>
			等價于
			<strong>很复杂很复杂.....</strong>
		</form>

		<!-- 模板內部变量申明 -->
		<c:var name="中文名">${user.姓氏}${user.大名}</c:var>
		<c:var name="englishName"
			value="${user.firstName}.${user.lastName}" />
		<p>我的中文名字是：${中文名}</p>
		<p>我的英文名字是：${englishName}</p>
	</body>
</html>
